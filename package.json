{
  "name": "plantuml-preview",
  "displayName": "PlantUML Preview",
  "description": "A Live PlantUML Preview using browser scripts.",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "languages": [
      {
        "id": "plantuml",
        "extensions": [
          ".puml",
          ".pu",
          ".plantuml"
        ],
        "aliases": [
          "PlantUML"
        ]
      }
    ],
    "commands": [
      {
        "command": "plantuml-preview.start",
        "title": "PlantUML: Open Live Preview",
        "icon": "$(open-preview)"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "plantuml-preview.start",
          "when": "editorLangId == plantuml",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.80.0",
    "@types/node": "18.x",
    "typescript": "^5.1.3"
  }
}
